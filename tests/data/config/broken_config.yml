# yaml-language-server: $schema=broken_config.schema.json

plotting:
  general:
    default_args_override:
      will_update_arg: will change arg
      will_be_kept_arg: general arg
    axis_label_override:
      will_update_label: will change label
      will_be_kept_label:
        not_allowed_general: False
    invalid_kw_general: True

  test_func:
    default_args_override:
      will_update_arg: test_func arg
      will_be_added_arg: new arg
    axis_label_override:
      will_update_label: test_func label
      will_be_added_label:
        not_allowed_test_func: False
    invalid_kw_test_func: True

invalid_kw_root: True
